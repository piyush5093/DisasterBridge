# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Database / ODM (MongoDB) ---
motor==3.7.1
beanie==1.29.0
pymongo==4.17.0

# --- Geometry helpers (still used for centroid reduction of GDACS
#     polygon/line features before storing as a GeoJSON Point — see
#     gdacs_ingestion.py) ---
shapely==2.0.6

# --- Async jobs (optional, used from Day 4/5 onward) ---
celery==5.4.0
redis==5.0.8

# --- HTTP / external feeds (GDACS, USGS, NDMA, Sentinel Hub, etc.) ---
httpx==0.27.2

# --- Data science / geo (used from Week 3 onward, added now so env is ready) ---
pandas==2.2.2
numpy==1.26.4
geopandas==1.0.1
rasterio==1.3.10

# --- Dev / testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
python-dotenv==1.0.1
